plugins {
    id 'java'
}

group = 'com.eden'
version = "${versions.common}"

repositories {
    mavenCentral()
}

dependencies {

    implementation "org.apache.commons:commons-lang3:${versions.lang}"
}
