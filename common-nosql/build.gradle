plugins {
    id 'java'
}

group = 'com.eden'
version = "${common}"

repositories {
    mavenCentral()
}

dependencies {
    compileOnly "org.springframework.data:spring-data-commons:${springData}"
    compileOnly "org.mongodb:bson:${bson}"
}
